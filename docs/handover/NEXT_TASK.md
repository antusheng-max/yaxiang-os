# 下一任务

## 物理设备受控测试 (需人工确认)

### 前置条件
- [x] V0.3-dev系统镜像构建完成
- [x] V0.3-dev安装ISO生成完成
- [x] 93项QEMU测试全部通过
- [x] 真实功能完成率94%
- [x] 122个rpcd端点就绪

### 任务清单

1. **物理PPPoE拨号验证**
   - 连接真实宽带线路
   - 验证PPPoE认证/获取IP/断线重拨
   - 验证多拨并发

2. **带宽探测物理验证**
   - 验证实际运营商限速点
   - 验证有效吞吐探测
   - 验证回退逻辑

3. **IP前缀数据库导入**
   - 获取可靠IP前缀数据源
   - 导入CSV
   - 验证省份/运营商匹配

4. **多WAN物理验证**
   - 多条真实宽带
   - 故障切换
   - 会话保持

### 注意事项
- 必须人工在物理服务器前操作
- 不得远程自动执行
- 安装前备份目标磁盘
# 下一任务

## 构建V0.3系统镜像与安装ISO

### 前置条件
- [x] 5个rpcd脚本完成 (78端点)
- [x] realAdapter.js对接完成
- [x] Web生产构建通过
- [x] 功能矩阵更新 (65%真实接入)
- [ ] OpenWrt构建环境可用

### 任务清单

1. **构建V0.3 OpenWrt镜像**
   - 将新rpcd脚本加入构建
   - 将新Web产物加入yaxiang-web包
   - 编译combined.img.gz + combined-efi.img.gz
   - 校验软件包安装

2. **生成V0.3安装ISO**
   - 使用新镜像替换installer/images/
   - 重新执行build-installer-iso.sh
   - BIOS/UEFI启动验证
   - 端到端安装验证

3. **QEMU验证新rpcd端点**
   - 启动V0.3镜像
   - 验证78个端点响应
   - 验证LAN/WAN写入生效
   - 验证重启后配置保持

4. **继续实现UNIMPLEMENTED功能**
   - VLAN 802.1Q
   - 多WAN调度
   - 流控
   - 健康检查

### 注意事项
- 不修改构建服务器网络
- 不操作真实块设备
- 保留V0.2-dev基线
# 下一任务

## 第六阶段B - LAN/WAN真实配置开发

### 前置条件
- [x] 网络控制底座 netctl.sh 完成
- [x] 基础网卡真实读取已接入
- [x] rpcd/realAdapter 对接完成
- [x] 安全测试通过

### 任务清单

1. **LAN真实配置**
   - 读取当前LAN IP/子网/网关
   - 修改LAN IP地址
   - 修改子网掩码
   - 应用配置 (netifd reload)
   - 失败回滚

2. **WAN真实配置**
   - 读取当前WAN proto/ipaddr/gateway
   - 切换proto (dhcp/static/pppoe)
   - 静态IP配置写入
   - DHCP模式配置
   - 应用与回滚

3. **接口管理**
   - 接口列表真实读取
   - 接口启用/禁用
   - 接口状态实时监控

4. **QEMU端到端验证**
   - OpenWrt VM中修改LAN IP
   - 验证配置持久化
   - 验证回滚机制

### 注意事项
- 所有写入测试仅在QEMU OpenWrt VM中
- 不修改构建服务器网络
- 不修改服务器UCI配置
- 不重启服务器网络
# 下一任务

## 物理设备受控测试 (需人工确认)

### 前置条件
- [x] 五个阶段虚拟测试全部通过
- [x] Legacy BIOS + UEFI 端到端验证通过
- [x] 安装 ISO 完整可用 (569MB)
- [x] 13项端到端测试全部通过

### 任务清单

1. **物理设备安装准备**
   - 确认目标服务器 (戴尔 R730XD)
   - 备份目标磁盘数据
   - 确认 iDRAC 串口控制台可用
   - 准备 USB 启动盘或虚拟介质

2. **受控安装执行**
   - 从 ISO 启动 (USB/iDRAC虚拟介质)
   - 选择目标磁盘 (非系统盘)
   - 完成双重确认 + INSTALL
   - 验证写入和校验

3. **安装后验证**
   - hostname: Yaxiang-Gateway
   - LAN: 192.168.1.1
   - uHTTPd + rpcd 运行
   - Web 界面可访问
   - /etc/yaxiang-release 存在

### 注意事项
- 必须由人工在物理服务器前确认
- 不得远程自动执行物理安装
- 安装前必须备份目标磁盘
- 不修改服务器网络配置直到验证完成
# 下一任务

## 安装 ISO 第五阶段 - ISO 端到端安装测试

### 前置条件
- [x] Live 环境骨架完成
- [x] 磁盘识别与安全确认完成 (第二阶段)
- [x] 镜像写入与校验完成 (第三阶段)
- [x] ISO 生成与启动验证完成 (第四阶段)
- [x] BIOS + UEFI 双启动 ISO 可用
- [x] 10项 QEMU 启动测试全部通过

### 任务清单

1. **QEMU 端到端安装测试**
   - 从 ISO 启动进入 Live 环境
   - 运行安装器完整流程
   - 磁盘选择 + 双重确认 + INSTALL
   - 镜像写入 + 写后校验
   - 重启后从目标盘启动验证

2. **Legacy BIOS 端到端**
   - ISO 启动 -> 安装 -> 重启 -> 系统运行

3. **UEFI 端到端**
   - ISO 启动 -> 安装 -> 重启 -> 系统运行

4. **异常场景测试**
   - 安装中断电恢复
   - 目标盘容量不足
   - 镜像损坏

### 注意事项
- 不得在开发服务器上执行真实写盘
- 使用 QEMU 虚拟磁盘测试
- 不关闭 8080/8081 服务
- 不在物理服务器安装
# 下一任务

## 安装 ISO 第四阶段 - 最终 ISO 制作

### 前置条件
- [x] Live 环境骨架完成
- [x] 系统镜像完整
- [x] 磁盘识别与安全确认完成 (第二阶段)
- [x] 镜像写入与校验完成 (第三阶段)
- [x] Legacy + UEFI 写入后启动验证通过
- [x] 15项 QEMU 测试全部通过

### 任务清单

1. **El Torito BIOS 引导镜像**
   - 使用 grub-mkimage 创建 cdboot.img
   - 验证 BIOS 引导

2. **EFI 系统分区结构**
   - 创建 EFI/BOOT/BOOTX64.EFI
   - 验证 UEFI 引导

3. **最终 ISO 制作**
   - 使用 xorriso/grub-mkrescue
   - BIOS + UEFI 双引导
   - ISO 完整性校验

4. **QEMU 完整验证**
   - 从 ISO 启动 Live 环境
   - 执行完整安装流程
   - 验证安装后启动

### 注意事项
- 不得在开发服务器上执行真实写盘
- 使用 QEMU 虚拟磁盘测试
- 不关闭 8080/8081 服务
- 不在物理服务器安装
# 下一任务

## 安装 ISO 第三阶段 - 镜像写入与校验

### 前置条件
- [x] Live 环境骨架完成
- [x] 系统镜像完整
- [x] QEMU 启动验证通过
- [x] 磁盘识别与安全确认完成 (第二阶段)
- [x] 双重确认机制验证通过
- [x] 身份哈希核对机制验证通过

### 任务清单

1. **系统镜像写入**
   - 使用 dd + gzip 解压写入
   - 写入进度显示 (百分比 + 速度)
   - 写入完成后 sync

2. **写入后校验**
   - SHA256 校验写入的镜像
   - 与源镜像哈希比对
   - 校验失败时报警

3. **GRUB 安装到目标磁盘**
   - BIOS: grub-install 到 MBR
   - UEFI: 创建 EFI 系统分区 + grub-install

4. **分区方案**
   - GPT 分区表 (UEFI)
   - MBR 分区表 (BIOS)
   - 自动调整分区大小

5. **错误处理**
   - 写入中断恢复
   - 空间不足检测
   - 设备断开检测

6. **最终 ISO 制作**
   - El Torito BIOS 引导镜像
   - EFI 系统分区结构
   - 使用 xorriso/grub-mkrescue
   - BIOS + UEFI 双引导
   - QEMU 完整验证

### 注意事项
- 不得在开发服务器上执行真实写盘
- 使用 QEMU 虚拟磁盘测试
- 每步操作需有回滚机制
- 写入前必须通过第二阶段的双重确认
# 下一任务

## 安装 ISO 第二阶段 - 真实安装器开发

### 前置条件
- [x] Live 环境骨架完成
- [x] 系统镜像完整
- [x] QEMU 启动验证通过

### 任务清单

1. **El Torito BIOS 引导镜像**
   - 使用 grub-mkimage 创建 cdboot.img
   - 验证 BIOS 引导

2. **EFI 系统分区结构**
   - 创建 EFI/BOOT/BOOTX64.EFI
   - 验证 UEFI 引导

3. **真实安装器写盘逻辑**
   - 磁盘检测和选择
   - 分区方案 (GPT/MBR)
   - 系统镜像写入 (dd + gzip)
   - 写入后校验
   - GRUB 安装到目标磁盘

4. **安装器 UI**
   - 磁盘选择界面
   - 确认对话框
   - 进度显示
   - 完成提示

5. **最终 ISO 制作**
   - 使用 xorriso/grub-mkrescue
   - BIOS + UEFI 双引导
   - QEMU 完整验证

### 注意事项
- 不得在开发服务器上执行真实写盘
- 使用 QEMU 虚拟磁盘测试
- 每步操作需有回滚机制
