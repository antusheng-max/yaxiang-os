# 安全政策

## 报告安全问题

如发现安全漏洞，请通过以下方式私下报告：

- 发送电子邮件至仓库所有者
- 不要创建公开的Issue

请在报告中包含：

- 漏洞描述
- 复现步骤
- 影响范围
- 建议修复方案（可选）

## 凭据安全原则

- ❌ 不得在代码中硬编码密码、Token或API密钥
- ❌ 不得提交SSH私钥或证书文件
- ❌ 不得在日志中输出敏感信息
- ❌ 不得在Issue或PR中公开凭据
- ✅ 使用环境变量或配置文件管理敏感信息
- ✅ 将敏感配置文件加入.gitignore

## 网络控制模块安全原则

### 命令注入防护

- 所有用户输入必须经过白名单验证
- 禁止直接拼接用户输入到shell命令
- 使用参数化调用或转义处理

### 配置变更安全

- 所有UCI配置变更前自动备份
- 支持配置回滚机制
- 文件锁防止并发修改
- dry-run模式支持预览变更

### 密码处理

- PPPoE密码在API响应中脱敏显示
- 密码修改使用chpasswd安全接口
- 不在日志中记录密码明文

## 磁盘安装安全原则

### 安装介质保护

- 自动识别并排除安装介质
- 10种排除规则防止误写
- SHA256身份哈希验证

### 写入前确认

- 双重确认机制（大写Y + 设备编号）
- 显示完整磁盘信息供用户核对
- 重新扫描验证设备身份

### 写入后校验

- 完整字节级SHA256校验
- 校验失败自动报告
- 分区表重读验证

## 测试安全

- ⚠️ 不得直接在生产网络测试
- ⚠️ 网络写入测试必须在QEMU或隔离环境中完成
- ⚠️ 不得修改构建服务器真实网络配置
- ✅ 使用QEMU虚拟磁盘进行安装测试
- ✅ 使用测试模式标记防止误操作

## 支持版本

| 版本 | 支持状态 |
|------|----------|
| V0.3-dev | 开发测试版 |
| V0.2-dev | 基线版本 |
